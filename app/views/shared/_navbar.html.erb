<nav class="navbar navbar-expand-lg navbar-light bg-light-info">
  <div class="container">
    <a class="navbar-brand text-uppercase" href="/">Classroom</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <%= link_to "Courses", courses_path, class: "nav-link" %>
        </li>
      </ul>
      <ul class="navbar-nav ml-auto">
        <% if current_user %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= fa_icon "user", text: "#{current_user.name}" %>
            </a>
            <div class="dropdown-menu dropdown-menu-right bg-light-info" aria-labelledby="navbarDropdown">
              <%= link_to fa_icon("edit", text: "Edit profile"), edit_user_registration_path, class: "dropdown-item bg-light-info" %>
              <div class="dropdown-divider"></div>
              <%= link_to fa_icon("sign-out", text: "Logout"), destroy_user_session_path, method: :delete, class: "dropdown-item bg-light-info" %>
            </div>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to fa_icon("user-plus", text: "Signup"), new_user_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to fa_icon("sign-in", text: "Login"), new_user_session_path, class: "nav-link ml-2" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
